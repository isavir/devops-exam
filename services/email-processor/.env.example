# AWS Configuration
AWS_REGION=us-west-2
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# SQS Configuration - Retrieved from SSM Parameter Store
SQS_QUEUE_URL_PARAMETER=/email-service/sqs-queue-url

# Note: SQS_QUEUE_URL is now retrieved from SSM Parameter Store
# The parameter /email-service/sqs-queue-url contains the actual queue URL

# S3 Configuration - Retrieved from SSM Parameter Store
S3_BUCKET_NAME_PARAMETER=/email-service/s3-bucket-name

# Note: S3_BUCKET_NAME is now retrieved from SSM Parameter Store
# The parameter /email-service/s3-bucket-name contains the actual bucket name

# Processing Configuration
POLL_INTERVAL_SECONDS=30
MAX_MESSAGES_PER_POLL=10
VISIBILITY_TIMEOUT_SECONDS=300